# URL API Orkay (server Express). Default lokal.
ORKAY_API_URL=http://localhost:3001

# PIN untuk login ke Orkay (sama dengan APP_PIN di server/.env)
ORKAY_PIN=123456

# (opsional) berapa lama cache bootstrap dianggap fresh, dalam milidetik
BOOTSTRAP_TTL_MS=30000
